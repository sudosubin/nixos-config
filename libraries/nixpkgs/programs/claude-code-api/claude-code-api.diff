diff --git a/claude-code-api/src/core/config.rs b/claude-code-api/src/core/config.rs
index 38d74ee..e6aac33 100644
--- a/claude-code-api/src/core/config.rs
+++ b/claude-code-api/src/core/config.rs
@@ -67,6 +67,8 @@ pub struct ProcessPoolConfig {
     pub size: usize,
     pub min_idle: usize,
     pub max_idle: usize,
+    pub idle_timeout_secs: u64,
+    pub default_model: String,
 }
 
 impl Default for ProcessPoolConfig {
@@ -75,6 +77,8 @@ impl Default for ProcessPoolConfig {
             size: 5,
             min_idle: 2,
             max_idle: 5,
+            idle_timeout_secs: 300,
+            default_model: "claude-sonnet-4-20250514".to_string(),
         }
     }
 }
diff --git a/claude-code-api/src/main.rs b/claude-code-api/src/main.rs
index 700d688..c4071b2 100644
--- a/claude-code-api/src/main.rs
+++ b/claude-code-api/src/main.rs
@@ -70,8 +70,8 @@ async fn create_app(settings: Settings) -> Result<Router> {
         min_idle: settings.process_pool.min_idle,
         max_idle: settings.process_pool.max_idle,
         max_active: settings.process_pool.size,
-        idle_timeout_secs: 300,
-        default_model: "claude-sonnet-4-20250514".to_string(),
+        idle_timeout_secs: settings.process_pool.idle_timeout_secs,
+        default_model: settings.process_pool.default_model.clone(),
     };
     
     // 初始化进程池
diff --git a/claude-code-api/src/models/claude.rs b/claude-code-api/src/models/claude.rs
index 0305d04..35857be 100644
--- a/claude-code-api/src/models/claude.rs
+++ b/claude-code-api/src/models/claude.rs
@@ -93,21 +93,57 @@ pub struct ClaudeModel {
 impl ClaudeModel {
     pub fn all() -> Vec<Self> {
         vec![
+            // Claude 4.5 Series (2025)
+            Self {
+                id: "claude-opus-4-5-20251101".to_string(),
+                display_name: "Claude Opus 4.5".to_string(),
+                context_window: 200000,
+            },
+            Self {
+                id: "claude-opus-4-5".to_string(),
+                display_name: "Claude Opus 4.5 (Latest)".to_string(),
+                context_window: 200000,
+            },
+            Self {
+                id: "claude-sonnet-4-5-20250929".to_string(),
+                display_name: "Claude Sonnet 4.5".to_string(),
+                context_window: 200000,
+            },
+            Self {
+                id: "claude-sonnet-4-5".to_string(),
+                display_name: "Claude Sonnet 4.5 (Latest)".to_string(),
+                context_window: 200000,
+            },
+            Self {
+                id: "claude-haiku-4-5-20251001".to_string(),
+                display_name: "Claude Haiku 4.5".to_string(),
+                context_window: 200000,
+            },
+            Self {
+                id: "claude-haiku-4-5".to_string(),
+                display_name: "Claude Haiku 4.5 (Latest)".to_string(),
+                context_window: 200000,
+            },
             // Claude 4 Series (2025)
             Self {
                 id: "claude-opus-4-1-20250805".to_string(),
                 display_name: "Claude Opus 4.1".to_string(),
-                context_window: 500000,
+                context_window: 200000,
+            },
+            Self {
+                id: "claude-opus-4-1".to_string(),
+                display_name: "Claude Opus 4.1 (Latest)".to_string(),
+                context_window: 200000,
             },
             Self {
                 id: "claude-opus-4-20250514".to_string(),
                 display_name: "Claude Opus 4".to_string(),
-                context_window: 500000,
+                context_window: 200000,
             },
             Self {
                 id: "claude-sonnet-4-20250514".to_string(),
                 display_name: "Claude Sonnet 4".to_string(),
-                context_window: 500000,
+                context_window: 200000,
             },
             // Claude 3.7 Series (2025)
             Self {
diff --git a/claude-code-api/src/models/tests.rs b/claude-code-api/src/models/tests.rs
index 50cebaa..cb8939e 100644
--- a/claude-code-api/src/models/tests.rs
+++ b/claude-code-api/src/models/tests.rs
@@ -20,11 +20,19 @@ mod tests {
     #[test]
     fn test_claude_model_list() {
         let models = ClaudeModel::all();
-        assert_eq!(models.len(), 8); // 3 Claude 4 + 2 Claude 3.7 + 2 Claude 3.5 + 1 Claude 3
+        assert_eq!(models.len(), 15); // 6 Claude 4.5 + 4 Claude 4 + 2 Claude 3.7 + 2 Claude 3.5 + 1 Claude 3
         
         let model_ids: Vec<String> = models.iter().map(|m| m.id.clone()).collect();
+        // Check Claude 4.5 models
+        assert!(model_ids.contains(&"claude-opus-4-5-20251101".to_string()));
+        assert!(model_ids.contains(&"claude-opus-4-5".to_string()));
+        assert!(model_ids.contains(&"claude-sonnet-4-5-20250929".to_string()));
+        assert!(model_ids.contains(&"claude-sonnet-4-5".to_string()));
+        assert!(model_ids.contains(&"claude-haiku-4-5-20251001".to_string()));
+        assert!(model_ids.contains(&"claude-haiku-4-5".to_string()));
         // Check Claude 4 models
         assert!(model_ids.contains(&"claude-opus-4-1-20250805".to_string()));
+        assert!(model_ids.contains(&"claude-opus-4-1".to_string()));
         assert!(model_ids.contains(&"claude-opus-4-20250514".to_string()));
         assert!(model_ids.contains(&"claude-sonnet-4-20250514".to_string()));
         // Check Claude 3.7 models
